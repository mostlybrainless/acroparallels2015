
#define SIZE_TABLE_INFO (sizeof(size_t) * 8)	// use 64 bits to write size of Huffman's table
#define SIZE_HCODE 4							// use 4 bits to write the size of Huffman's code 
#define SIZE_CHAR (sizeof(char) * 8)			// 8 bits